import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AirLOQC - Web3 Privacy Solutions",
  description: "Explore cutting-edge Web3 privacy solutions with AirLOQC. Our platform leverages zero-knowledge proofs to ensure secure and private transactions in the blockchain ecosystem.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-[#121315]`}
      >
        {children}
      </body>
    </html>
  );
}
